{{ 'header.css' |  asset_url |  stylesheet_tag }}


<header class="nav-bar__container">
  <div class="nav-bar__items">
    <a href="#" class="site-nav__link--icon">
      {%  render 'icon-hamburger' %}
    </a>
    <a href="/">
      <img src="{{ section.settings.desktop_logo |  image_url: width: section.settings.desktop_logo.width }}" class="header__brand-logo">
    </a>
    {% comment %} <a href="{{ routes.cart_url }}" class="site-nav__link site-nav__link--icon js-drawer-open-cart" aria-controls="CartDrawer">
      {%  render 'icon-cart-bag' %} 
    </a> {% endcomment %}
    <a href="{{ routes.cart_url }}" class="site-nav__link site-nav__link--icon js-drawer-open-cart" aria-controls="CartDrawer" data-icon="{{ settings.cart_icon }}">
      {%  render 'icon-cart-bag' %}
    </a>
  </div>
  <div class="nav-drawer__container">
    <div class="nav-drawer__header">
      <img src="{{ section.settings.desktop_logo |  image_url: width: section.settings.desktop_logo.width }}" class="header__brand-logo">
      <span class="nav-drawer__close-btn">
        {%  render 'icon-cross' %} 
      </span>
    </div>
    {% for block in section.blocks %} 
      {% case block.type %}
        {% when 'shop_info' %}
          <div class="shop__info-wrapper">
            <h2>{{ block.settings.title }}</h2>
            <div>
              <p><span></span> {{ block.settings.description_1 }}</p>
              <p><span></span> {{ block.settings.description_2 }}</p>
            </div>
          </div> 
        {% when 'product_card' %}
          <div class="nav-drawer__product-card">
            
          </div>
      {% endcase %}
    {% endfor %}
  </div>
</header>

{%  schema  %} 
{
  "name": "Header",
  "settings": [
    {
      "type": "image_picker",
      "id": "desktop_logo",
      "label": "Logo"
    }
  ],
  "blocks": [
    {
      "name": "Shop Info",
      "type": "shop_info",
      "settings": [
        {
          "type": "inline_richtext",
          "id": "title",
          "label": "Title",
          "default": "Title"
        },
        {
          "type": "inline_richtext",
          "id": "description_1",
          "label": "Description One",
          "default": "Description"
        },
        {
          "type": "inline_richtext",
          "id": "description_2",
          "label": "Description Two",
          "default": "Description"
        }
      ]
    }, 
    {
      "name": "Product",
      "type": "product_card",
      "settings": [
        {
          "type": "product",
          "id": "product",
          "label": "Product"
        },
        {
          "type": "text",
          "id": "product_badge",
          "label": "Product Badge"
        }
      ]
    }
  ]
}


{% endschema %}